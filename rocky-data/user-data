#cloud-config
users:
  - name: rocky
    plaintext_passwd: changeme
    lock_passwd: false
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
ssh_pwauth: true
chpasswd:
  list: |
    rocky:changeme
  expire: false
